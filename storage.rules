rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // Allow authenticated users to access recordings
    match /recordings/{familyCode}/{allPaths=**} {
      allow read, write: if request.auth != null;
    }
  }
}